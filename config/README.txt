ymlファイルについて

ymlファイルでは、STLDesigner3が最初の初期化処理を行う際の
初期値を設定します。
どのようなパラメータがあるかは"default.yml"を参考にしてください

ymlファイルでは、は空白やインデント揃えにも見やすさだけでなく文法的な意味があるので注意してください。
 : や - にも文法的な意味があります．
行中に # があると，同じ行のそれ以降の文字はコメントとなります。
default.ymlをコピーして変更したい部分のみを変えるようにするといいと思います。
